/**
 ******************************************************************************
 * @file           : main.c
 * @author         : Auto-generated by STM32CubeIDE
 * @brief          : Main program body
 ******************************************************************************
 * @attention
 *
 * Copyright (c) 2024 STMicroelectronics.
 * All rights reserved.
 *
 * This software is licensed under terms that can be found in the LICENSE file
 * in the root directory of this software component.
 * If no LICENSE file comes with this software, it is provided AS-IS.
 *
 ******************************************************************************
 */

#include <stdint.h>
#include "bitwise.h"

#if !defined(__SOFT_FP__) && defined(__ARM_FP)
  #warning "FPU is not initialized, but the project is compiling for an FPU. Please initialize the FPU before use."
#endif

int main(void)
{
	// the base address of RCC: `0x40023800`
	// Address offset: 0x30
	RCC_AHB1ENR_t volatile *pClockControlRegister =  (RCC_AHB1ENR_t*) (0x40023800 + 0x30);
	// the base address of GPIOD mode register: `0x40020C00`
	// Address offset: 0x00
	GPIOx_MODER_t volatile *pGPIODModeRegister = (GPIOx_MODER_t*) (0x40020C00 + 0x00);
	// the base address of GPIOD output data register: `0x40020C00`
	// Address offset: 0x14
	GPIOx_ODR_t volatile *pGPIODOutputDataRegister = (GPIOx_ODR_t*)(0x40020C00 + 0x14);

	// 1. enable the clock for the GPIOD peripheral in AHB1ENR
	pClockControlRegister->gpiod_en = 1;
	// step 2. configure the  GPIOD_MODER to output mode for pd12
	pGPIODModeRegister->moder_12 = 1;

    /* Loop forever */
	while(1) {
		// step 3. SET the 12th pin of the GPIO register
		pGPIODOutputDataRegister->odr_12 = 1;

		// delay
		// this is a software delay
		// this is not very precise
		// use hardware delays such as timers for more precision
		for(uint32_t i=0;i<100000;i++);

		// turn off led
		pGPIODOutputDataRegister->odr_12 = 0;

		// delay
		for(uint32_t i=0;i<100000;i++);
	}
}
